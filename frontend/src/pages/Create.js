import { useAuth0 } from "@auth0/auth0-react";
import React from "react";

export default function Create() {
    return (
        <div className="mt-3">
            Create a club
        </div>
    )
}